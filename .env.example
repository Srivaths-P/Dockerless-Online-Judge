# This is an example environment file.
# To get started, copy this file to a new file named .env and fill in your own secret values.
# The .env file should NEVER be committed to version control.

# --- Security ---
# IMPORTANT: Both secret keys should be long, random, and secret strings.
# You can generate them using: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=
SESSION_SECRET_KEY=

# --- JWT Configuration ---
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Database ---
# The default SQLite database URL.
DATABASE_URL=sqlite:///./judge.db
